"""This is only here because pytest wasn't finding the package,
even after installing with `pip install -e .`
Note: this is a VS Code bug: running `pytest` on the terminal
works fine without this file.
"""
